function NoBookmarks(props) {
    const styles = props.styles;

    return (
        <>
        <p className={styles.message}>Begin adding bookmarks by clicking the category menu above.</p>
        </>
    );
  }

export default NoBookmarks;